一维卷积 什么是卷积运算（此处应有一维离散卷积操作运算实例）
卷积运算的意义是什么 总的来说，卷积可以提取信号中某种特性的信息（特征）

二维卷积 二维数组+二维卷积核
卷积核大小 卷积步长 （此处应有二维卷积运算集体步骤）
二维卷积的应用 
人为设计卷积核 -> 参数可学习卷积（用于语意信息丰富的特征）

全连接网络

图像处理为什么要用卷积神经网络？ 降低模型复杂度
稀疏连接 局部特征 全局特征 
平移不变性 全连接网络（拉成一位向量，空间信息损失）/卷积神经网络（性能更优，速度更快，参数量更少）
练习：MINST手写数据集训练

卷积神经网络特征可视化

LeNet & AlexNet VGG

卷积神经网络的感受野 实现同样的感受野，多个小卷积堆叠比一个大卷积核使用的参数更少（此处应有举例）

GoogLeNet 引入Inception结构（融合不同尺度的特征信息，小尺度特征，中尺度特征，大尺度特征）（此处应有举例）

ResNet 
梯度爆炸和梯度消失——导致网络难做
梯度消失：某一层的误差梯度趋近于0,反向传播时，整体梯度趋近于0
梯度爆炸：某一层的误差非常大，反向传播时，整体梯度非常大
提出残差结构减轻退化问题，进一步加深网络->解决了深层网络的训练问题，加速了网络训练过程的收敛，实现了浅层特征复用
特点：学习100->101很难 但是学习0->1很简单

卷积神经网络的应用 High Level and Low Level
超分辨SR3 去噪FastDVDnet 目标检测YOLO 语义分割DeepLab

简单神经网络的构建 
输入图像 3乘3乘1 
卷积层：卷积核1 2 3 ... 64 都为3乘3乘1 卷积层一共为64乘1乘3乘3
输出特征 3乘3乘64

卷积神经网络的w和b
输入数据[1, 5, 5] (注意要补0) -> 第一层卷积层：输入通道数为1 输出通道数为16 卷积核为3 W[16, 1, 3, 3] B[16 ] -> 第一层卷积层输出[16, 5, 5] -> 第二层卷积层：输入通道数为16 输出通道数为N 卷积核为3 W[N, 16, 3, 3] B[N ] -> 第二层卷积层输出[N, 5, 5]  （特征图尺寸[通道数， 宽度， 高度]）

处理大尺寸二维矩阵时，小卷积优于全连接