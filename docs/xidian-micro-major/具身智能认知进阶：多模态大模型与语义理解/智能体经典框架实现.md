# 智能体经典框架实现

> 西安电子科技大学 · 具身智能微专业
> 课程笔记

---

## 一、ReAct Agent——从零实现

### 1.1 核心循环

```python
import re
import json
from typing import Dict, Callable, List, Tuple

class ReActAgent:
    """ReAct Agent 完整实现——250 行搞定"""

    def __init__(self, llm_call: Callable, tools: Dict[str, Callable],
                 max_steps: int = 10, verbose: bool = True):
        self.llm = llm_call          # def llm_call(prompt: str) -> str
        self.tools = tools           # {"search": func, "calc": func}
        self.max_steps = max_steps
        self.verbose = verbose
        self.history: List[Tuple[str, str]] = []

    def run(self, query: str) -> str:
        for step in range(self.max_steps):
            if self.verbose:
                print(f"\n{'='*40}\nStep {step+1}/{self.max_steps}")

            prompt = self._build_prompt(query)
            response = self.llm(prompt)

            if self.verbose:
                print(f"LLM Response:\n{response[:200]}...")

            # 解析 Thought 和 Action
            parsed = self._parse(response)
            if parsed is None:
                self.history.append(("error", "解析失败，请重试"))
                continue

            thought, action_type, action_name, action_input = parsed
            self.history.append(("thought", thought))

            if self.verbose:
                print(f"Thought: {thought}")

            # Agent 决定结束
            if action_type == "FINISH":
                return action_input

            # 执行工具
            if action_name not in self.tools:
                obs = f"错误: 工具 '{action_name}' 不存在。可用: {list(self.tools.keys())}"
            else:
                try:
                    result = self.tools[action_name](action_input)
                    obs = str(result)
                except Exception as e:
                    obs = f"工具执行错误: {e}"

            self.history.append(("action", f"{action_name}({action_input})"))
            self.history.append(("observation", obs))

            if self.verbose:
                print(f"Action: {action_name}({action_input})")
                print(f"Observation: {obs[:200]}")

        return "达到最大步数限制，任务未完成"

    def _parse(self, response: str):
        """解析 LLM 输出，提取 Thought 和 Action"""
        # 匹配 Thought
        thought_match = re.search(r"Thought:\s*(.+?)(?=\n(?:Action|Observation|$))",
                                  response, re.DOTALL)
        thought = thought_match.group(1).strip() if thought_match else ""

        # 匹配 FINISH
        finish_match = re.search(r"FINISH\((.+?)\)", response, re.DOTALL)
        if finish_match:
            return (thought, "FINISH", None, finish_match.group(1).strip())

        # 匹配 Action: tool_name(tool_input)
        action_match = re.search(r"Action:\s*(\w+)\((.+?)\)", response)
        if action_match:
            return (thought, "ACTION",
                    action_match.group(1), action_match.group(2).strip())

        return None

    def _build_prompt(self, query: str) -> str:
        tools_desc = "\n".join(
            f"  - {name}: {func.__doc__ or '无描述'}"
            for name, func in self.tools.items()
        )
        history_str = "\n".join(
            f"{role.capitalize()}: {content[:300]}"
            for role, content in self.history[-9:]  # 只保留最近 3 轮（9 条）
        )
        return f"""你是一个可以调用工具的 AI 助手，使用 ReAct 模式完成任务。

## 可用工具
{tools_desc}

## 历史记录
{history_str if history_str else "(无)"}

## 当前任务
{query}

## 输出格式（严格遵守）
Thought: [你对当前情况和下一步的思考]
Action: 工具名(参数)  ← 调用工具时使用
或
FINISH(最终答案)  ← 任务完成时使用
"""
```

### 1.2 工具定义

```python
import requests
import math

def search_web(query: str) -> str:
    """搜索网络信息。参数 query: 搜索关键词"""
    # 实际可用 DuckDuckGo / SerpAPI 等
    url = f"https://api.duckduckgo.com/?q={query}&format=json"
    try:
        resp = requests.get(url, timeout=10)
        data = resp.json()
        return data.get("AbstractText", "未找到相关信息")
    except:
        return f"搜索结果: 关于 '{query}' 的信息..."

def calculate(expression: str) -> str:
    """安全地计算数学表达式。参数 expression: 如 '2+3*4'"""
    allowed = set("0123456789+-*/().^% ")
    if not all(c in allowed for c in expression):
        return "错误: 表达式包含不允许的字符"
    # 安全 eval——禁止 builtins
    result = eval(expression, {"__builtins__": {}},
                  {"sqrt": math.sqrt, "pow": pow, "pi": math.pi})
    return f"{result}"

def read_file(path: str) -> str:
    """读取文件内容。参数 path: 文件路径"""
    try:
        with open(path, 'r') as f:
            return f.read(2000)  # 限制长度
    except FileNotFoundError:
        return f"错误: 文件 '{path}' 不存在"

# 组装 Agent
agent = ReActAgent(
    llm_call=lambda p: openai_call(p),  # 替换为真实 LLM 调用
    tools={"search": search_web, "calc": calculate, "read": read_file},
    max_steps=8
)
```

---

## 二、带记忆的 RAG Agent

```python
import numpy as np

class RAGAgent(ReActAgent):
    """在 ReAct 基础上增加向量记忆——Agent 能'记住'之前的查询结果"""

    def __init__(self, *args, embedding_model=None, **kwargs):
        super().__init__(*args, **kwargs)
        self.embed = embedding_model   # text → vector
        self.memory: List[Tuple[np.ndarray, str]] = []  # (embedding, content)

    def remember(self, content: str):
        """将重要信息存入长期记忆"""
        vec = self.embed(content)
        self.memory.append((vec, content))

    def recall(self, query: str, top_k: int = 3) -> str:
        """检索与 query 最相关的历史记忆"""
        if not self.memory:
            return "(无相关记忆)"
        q_vec = self.embed(query)
        scores = [np.dot(q_vec, m_vec) for m_vec, _ in self.memory]
        top_indices = np.argsort(scores)[-top_k:][::-1]
        return "\n".join(
            f"- {self.memory[i][1][:200]}" for i in top_indices
        )

    def run(self, query: str) -> str:
        # 先查记忆
        memory_context = self.recall(query)
        enhanced_query = f"相关历史记忆:\n{memory_context}\n\n当前任务: {query}"
        result = super().run(enhanced_query)
        # 把结果存到记忆中
        self.remember(f"Q: {query} → A: {result}")
        return result
```

---

## 三、LangGraph——有状态图 Agent

```python
from typing import TypedDict, Annotated
import operator

# 定义图状态
class AgentState(TypedDict):
    messages: Annotated[list, operator.add]  # 消息历史（自动追加）
    next_step: str                           # 下一步转到哪个节点
    final_answer: str                        # 最终答案

# 定义节点
def planner(state: AgentState) -> AgentState:
    """分析任务 → 决定是否需要搜索"""
    last_msg = state["messages"][-1]
    # LLM 判断: 需要外部信息？→ search；不需要 → answer
    decision = llm_judge(last_msg)  # "search" or "answer"
    return {"next_step": decision}

def search(state: AgentState) -> AgentState:
    """执行搜索 → 结果加入消息"""
    result = search_web(state["messages"][-1])
    return {"messages": [f"搜索结果: {result}"], "next_step": "planner"}

def answer(state: AgentState) -> AgentState:
    """综合所有信息 → 生成最终回答"""
    answer = llm_generate(state["messages"])
    return {"final_answer": answer, "next_step": "END"}

# 构建图
from langgraph.graph import StateGraph, END

graph = StateGraph(AgentState)
graph.add_node("planner", planner)
graph.add_node("search", search)
graph.add_node("answer", answer)

graph.set_entry_point("planner")
graph.add_conditional_edges("planner",
    lambda s: s["next_step"],  # → "search" or "answer"
    {"search": "search", "answer": "answer"})
graph.add_edge("search", "planner")  # 搜索完回到 planner 再判断
graph.add_edge("answer", END)

app = graph.compile()
result = app.invoke({"messages": ["北京今天的天气如何？"]})
print(result["final_answer"])
```

---

## 四、多 Agent 代码协作——AutoGen 风格

```python
class CodeAgent:
    """一个能写代码、运行代码、看结果、修正的 Agent"""

    def __init__(self, llm):
        self.llm = llm
        self.workspace = {}

    def solve(self, task: str, max_iterations: int = 5) -> str:
        prompt = f"""你是 Python 专家。用户任务: {task}

每次迭代:
1. 写 Python 代码并执行
2. 看执行结果
3. 决定: 修正代码 / 完成任务"""

        for i in range(max_iterations):
            # LLM 生成代码
            code = self.llm(f"{prompt}\n\n前次尝试:\n{self.workspace.get('last_result', '无')}")
            code = self._extract_code(code)

            # 在沙箱中执行
            result = self._execute(code)

            if result["success"] and self._check_complete(task, result):
                return f"任务完成!\n代码:\n{code}\n结果: {result['output']}"

            # 存结果，下轮迭代修正
            self.workspace["last_result"] = f"代码:\n{code}\n错误:\n{result['error']}"

        return "达到最大迭代次数"

    def _execute(self, code: str) -> dict:
        """在受限环境中执行 Python 代码"""
        import subprocess, tempfile
        with tempfile.NamedTemporaryFile(mode='w', suffix='.py', delete=False) as f:
            f.write(code)
        try:
            output = subprocess.run(
                ["python3", f.name], capture_output=True, text=True, timeout=30
            )
            return {"success": output.returncode == 0,
                    "output": output.stdout,
                    "error": output.stderr}
        except subprocess.TimeoutExpired:
            return {"success": False, "output": "", "error": "超时——可能有死循环"}
```

---

## 五、各框架选型指南

| 场景 | 推荐框架 | 理由 |
|------|---------|------|
| 学习 Agent 原理 | 手写 ReAct | 控制每一行，理解核心循环 |
| 快速原型验证 | LangChain | 组件齐全，文档丰富 |
| 复杂多步逻辑 | LangGraph | 有状态管控，可调试 |
| 多 Agent 协作 | AutoGen / CrewAI | 角色明确，通信协议现成 |
| 知识密集型 | LlamaIndex | 索引 + 检索是核心 |
| 生产部署 | OpenAI Agents SDK | Guardrails + 监控 + 追踪 |
| 产品化 | 自研轻量框架 | 依赖最少，性能最优 |

> 最终建议：学会 LangGraph 就够应对 90% 的 Agent 需求。LangGraph 是 Agent 框架的"终极形态"——图结构让它同时适合简单链和复杂多 Agent 协作。

---

*相关笔记：[Agent发展与经典框架](./Agent发展与经典框架.md)、[Agentic RL](./Agentic-RL.md)*
